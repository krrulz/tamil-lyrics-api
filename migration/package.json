{
  "name": "tamil-lyrics-migration",
  "version": "1.0.0",
  "description": "Admin migration scripts for Tamil Lyrics Firebase",
  "scripts": {
    "migrate":      "node migrate.js",
    "migrate:commit": "node migrate.js --commit",
    "update-meta":  "node update-metadata.js metadata-sample.csv",
    "update-meta:commit": "node update-metadata.js metadata-sample.csv --commit"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
